#ifndef SEARCH_REQ
    #define SEARCH_REQ
    #include <stdio.h>
    #include <stdlib.h>
    #include <strings.h>
#endif

void MergeSort(int A[], int l, int r);
void Merge(int A[], int l, int m, int r); 