#ifndef SEARCH_REQ
    #define SEARCH_REQ
    #include <stdio.h>
    #include <stdlib.h>
    #include <strings.h>
#endif

void MergeSort(int A[], int i, int j);
void Merge(int A[], int i, int m, int j); 